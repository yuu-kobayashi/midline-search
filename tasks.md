# tasks.md
> **プロジェクト目標**  
> Google検索結果ページのCSSを上書きし、検索結果のコンテンツを中央寄せにするChrome拡張機能を最小構成で開発・配布する。

---

## フェーズ 0 — プロジェクトの初期設定
| ID   | タスク                                     | 開始条件                   | 完了条件                                                     | 担当 |
| ---- | ------------------------------------------ | -------------------------- | ------------------------------------------------------------ | ---- |
| T00  | Gitリポジトリを初期化する。                | 作業用フォルダが空の状態。 | `git init`完了。最初の空コミットが`main`ブランチに存在。     |      |
| T01  | `.nvmrc`と`.editorconfig`を作成する。      | T00完了。                  | 設定ファイルをコミットし、VS Codeなどが自動認識。            |      |
| T02  | 最小限の`manifest.json`（MV3）を作成する。 | T01完了。                  | `chrome://extensions/`で「パッケージ化されていない拡張機能を読み込む」が成功。 |      |

---

## フェーズ 1 — コンテンツスクリプトのベース実装
| ID   | タスク                                                       | 開始条件  | 完了条件                                                 | 担当 |
| ---- | ------------------------------------------------------------ | --------- | -------------------------------------------------------- | ---- |
| T10  | `content.js`を作成し、`manifest.json`で読み込むようにする。  | T02完了。 | `google.com/search`でコンソールログが確認できる。        |      |
| T11  | `styles.css`に`#rcnt`を中央寄せするルール（`margin-inline:auto`）を1行記述。 | T10完了。 | 1280px以上で中央寄せが視覚的に確認できる。               |      |
| T12  | CSSを`manifest.json`経由で読み込む方式に変更（JSからの動的挿入をやめる）。 | T11完了。 | `content.js`なしでスタイルが適用され、動作が変わらない。 |      |

---

## フェーズ 2 — CSSの完成度向上
| ID   | タスク                                                       | 開始条件  | 完了条件                                            | 担当 |
| ---- | ------------------------------------------------------------ | --------- | --------------------------------------------------- | ---- |
| T20  | 1024px未満では従来の幅に戻すよう調整する。                   | T12完了。 | 360〜1920pxの間で左右スクロールが発生しない。       |      |
| T21  | `#center_col`やニュース枠などのモジュールも中央揃えにする。  | T20完了。 | すべてのモジュールが中央揃え、LighthouseでCLSゼロ。 |      |
| T22  | CSS適用時のみ`<body data-centered>`を付与してデバッグしやすくする。 | T21完了。 | フラグが正しく付与／削除され、DevToolsで確認可能。  |      |

---

## フェーズ 3 — カスタムCSSエディタの実装
| ID   | タスク                                                       | 開始条件  | 完了条件                             | 担当 |
| ---- | ------------------------------------------------------------ | --------- | ------------------------------------ | ---- |
| T30  | `options.html`のUIをスライダーからテキストエリアに変更       | T22完了。 | テキストエリアが表示され、プレースホルダーテキストが表示される。 |      |
| T31  | `options.js`を更新し、CSSテキストの保存機能を実装            | T30完了。 | テキストエリアの内容が`chrome.storage.sync`に保存される。 |      |
| T32  | `content.js`を更新し、保存されたCSSを動的に適用する機能を実装 | T31完了。 | 保存されたCSSが検索結果ページに反映される。 |      |
| T33  | エディタにシンタックスハイライトを追加                       | T32完了。 | CSSのシンタックスハイライトが機能する。 |      |
| T34  | エディタに自動インデント機能を追加                           | T33完了。 | 改行時に自動的にインデントが挿入される。 |      |
| T35  | エディタに基本的なエラーチェックを追加                       | T34完了。 | 無効なCSSが入力された場合に警告が表示される。 |      |

---

## フェーズ 4 — 仕上げとパッケージ化
| ID   | タスク                                                       | 開始条件           | 完了条件                                      | 担当 |
| ---- | ------------------------------------------------------------ | ------------------ | --------------------------------------------- | ---- |
| T40  | 128×128、48×48 のアイコンを用意する。                        | T35完了。          | アイコンがツールバーやWebストアで表示される。 |      |
| T41  | `README.md`を作成（インストール・開発・ビルド・テスト方法）。 | T40完了。          | 他人がREADMEだけで開発環境を再現できる。      |      |
| T42  | GitHub Actionsで`npm run lint`と`chrome-webstore-upload`のドライランを設定。 | T41完了。          | mainブランチマージでCIが通る。                |      |
| T43  | `CHANGELOG.md`を作成（keep-a-changelog形式）。               | T41完了。          | `v0.1.0`の初回エントリを追加。                |      |
| T44  | `v0.1.0`タグを作成し、`npm run build`でZIP出力。             | T43完了。          | `dist/`にZIPが生成され、公開準備完了。        |      |

---

## フェーズ 5 — 検証と公開
| ID   | タスク                                           | 開始条件         | 完了条件                                          | 担当 |
| ---- | ------------------------------------------------ | ---------------- | ------------------------------------------------- | ---- |
| T50  | Chrome（安定版・ベータ）、Edgeで手動テスト実施。 | T44完了。        | すべて動作し、コンソールにエラーなし。            |      |
| T51  | Chrome Web Storeへ「限定公開」で提出。           | T50完了。        | 拡張機能が「公開済み」ステータスに。              |      |
| T52  | フィードバック収集（最低3件のインストール）。    | T51完了後1週間。 | 改善点がIssueとして記録され、次スプリントが決定。 |      |

---

### テストチェックリスト（各タスクの参考）
1. **ページ読み込みテスト**：DevToolsでキャッシュ無効化後、再読み込みしてCSSが正しく適用されることを確認。  
2. **画面幅テスト**：360px〜1920pxまで幅をドラッグしながら表示崩れ・はみ出しをチェック。  
3. **モジュール表示確認**：検索結果にニュース・画像・動画・ナレッジパネルなどが出るクエリで表示確認。  
4. **アクセシビリティ**：文字色や背景とのコントラストが保たれているか確認。

---